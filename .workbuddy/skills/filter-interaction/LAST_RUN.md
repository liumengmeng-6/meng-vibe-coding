# filter-interaction Skill —— 最近一次检查记录

> 这份文件是检查脚本的真实输出，不是手写的总结。
> 重新跑一次脚本就会覆盖它。

- 运行时间：2026/9/25 11:41:49
- 命令：node .workbuddy/skills/filter-interaction/scripts/check-filter.js
- 场景：武汉 → 成都 / 重庆 / 西安，6 天，预算 4000（西安无内置数据，用来构造"筛出 0 条"）

A. 结构（规矩 1、4）
  [PASS] A1 筛选栏存在
  [PASS] A2 三个档位按钮都在 —— 实际 3 个
  [PASS] A3 三个都是真 <button>（白捡键盘能力：Tab 能到、回车能触发） —— BUTTON/BUTTON/BUTTON
  [PASS] A4 按钮组有 role="group" + 非空 aria-label（读屏会报"筛选清单，分组"）
  [PASS] A5 每个按钮都有 aria-pressed（读屏靠它知道选中的是哪一档） —— true/false/false
  [PASS] A6 条数元素存在且 aria-live="polite"（筛完会自动播报新数字）
  [PASS] A7 有「全部」这一档 —— 能筛进去也能回来，不是死路（规矩 1）

B. 默认状态
  [PASS] B1 默认选中「全部」
  [PASS] B2 未选中的两档 aria-pressed="false"
  [PASS] B3 默认显示全部 12 条（成都 6 + 重庆 6） —— 实际 12 条
  [PASS] B4 默认状态：吃的 6 条 + 玩的 6 条 —— 吃的 6 / 玩的 6
  [PASS] B5 条数文字说"共 N 条"且与实际行数一致（规矩 3：结果要有文字证据） —— 页面写"共 12 条"，实际 12 行

C. 准备"会被筛空"的城市（西安没有内置数据）
  [PASS] C1 往西安添加一条「吃的」成功
  [PASS] C2 清单多出 1 行 —— 12 → 13
  [PASS] C3 条数跟着变成 13（添加后条数自动刷新，没留在旧值） —— 页面写"共 13 条"

D. 筛「吃的」—— 有结果（规矩 2、3）
  [PASS] D1 「吃的」变成选中态 + aria-pressed="true"
  [PASS] D2 「全部」同时变成 aria-pressed="false"（选中态是唯一的）
  [PASS] D3 只剩 7 条（成都 3 + 重庆 3 + 西安 1） —— 实际 7 条
  [PASS] D4 一条"玩的"都不剩 —— 还剩 0 条
  [PASS] D5 条数文字变成"筛出 N 条"，且 N 与实际行数一致（规矩 3） —— 页面写"筛出 7 条"，实际 7 行

E. 筛「玩的」—— 有城市筛出 0 条（规矩 2，最容易漏的那条）
  [PASS] E1 表面结果只剩 6 条 —— 实际 6 条
  [PASS] E2 西安这个城市块还在（筛空了也不能把城市整块抹掉）
  [PASS] E3 西安块里出现了空状态 —— 不是白屏（规矩 2）
  [PASS] E4 空状态有明确文案 —— "这个筛选下没有条目 —— 西安一共有 1 条，都属于另一类。点上面的「全部」就能…"
  [PASS] E5 空状态里给出了出口（提示去点「全部」）
  [PASS] E6 西安块里确实没有表格了（不是"藏起来还占位置"）
  [PASS] E7 条数跟着变成 6（筛完立刻更新，不留旧数字） —— 页面写"筛出 6 条"

F. 点「全部」—— 恢复原状（规矩 1、2）
  [PASS] F1 13 条全回来了，一条不多一条不少 —— 实际 13 条
  [PASS] F2 吃 7 条、玩 6 条，跟筛之前一模一样 —— 吃的 7 / 玩的 6
  [PASS] F3 空状态消失
  [PASS] F4 按钮选中态回到「全部」
  [PASS] F5 条数文字从"筛出"变回"共 13 条" —— 页面写"共 13 条"

G. 连续操作（清单里的"连续操作测试"）
  [PASS] G1 连点「吃的」5 次，行数不变（不会越点越多） —— 实际 7 条
  [PASS] G2 连点后按钮状态没乱（只有一个选中）
  [PASS] G3 吃的→玩的→吃的→玩的→全部 来回切，最后仍完整恢复 —— 实际 13 条 / 共 13 条
  [PASS] G4 筛着的时候切方案：不报错、行数和条数依然对得上 —— 实际 7 条 / 筛出 7 条
  [PASS] G5 筛着的时候重新生成方案：不报错、行数和条数依然对得上 —— 实际 7 条 / 筛出 7 条
  [PASS] G6 重新生成没把用户选的档位弄丢（还是"吃的"）

H. 可访问性（余力加练：键盘 + 读屏 + 对比度）
  [PASS] H1 三个按钮都能拿到键盘焦点（Tab 能走到）
  [PASS] H2 写了 :focus-visible 规则（键盘给框、鼠标点完不留框）
  [PASS] H3 焦点提示用 outline 画（不靠改颜色糊弄） —— .filter-btn:focus-visible {
  outline: 2px solid var(--brand
  [PASS] H4 代码里真的在维护 aria-pressed（不是 HTML 里写死一次就不管了）
  [PASS] H5 选中态底色走 var(--brand)，没写死色值
  [PASS] H6 选中态文字色走 var(--on-brand) —— 两套主题各自适配（赛博是深字，白字在亮青上不达标）
  [PASS] H7 两套主题都定义了 --on-brand
  [PASS] H8 尊重"减少动态效果"系统设置（全局那条媒体查询还在）
  [PASS] H9 条数用等宽数字（切档位时数字不会左右抖）

H2. 对比度（规矩 5：正文 ≥ 4.5:1）
  [PASS] H10 选中档位的文字（明亮） —— #ffffff 压 #2a5fe0 = 5.51:1
  [PASS] H11 未选中档位的文字（明亮） —— #6b6459 压 #ffffff = 5.85:1
  [PASS] H12 条数文字（明亮） —— #6b6459 压 #f7f2e9 = 5.24:1
  [PASS] H13 「只看：」标签（明亮） —— #6a6459 压 #f7f2e9 = 5.26:1
  [PASS] H10 选中档位的文字（赛博） —— #04121a 压 #00e5ff = 12.34:1
  [PASS] H11 未选中档位的文字（赛博） —— #97a6c4 压 #0d1220 = 7.62:1
  [PASS] H12 条数文字（赛博） —— #97a6c4 压 #121a2a = 7.10:1
  [PASS] H13 「只看：」标签（赛博） —— #8291b0 压 #121a2a = 5.49:1

I. 回归（改动不能碰坏原有的东西）
  [PASS] I1 自填的行还有删除按钮（Day 7 的功能） —— 找到 1 个删除按钮
  [PASS] I2 点删除能删掉一条（筛着的时候也能删） —— 7 → 6
  [PASS] I3 Day 10 的「清空重填」确认条还在
  [PASS] I4 Day 11 的展开按钮样式还在
  [PASS] I5 美食清单卡片正常显示

========================================================
通过 60 项，失败 0 项，共 60 项
========================================================
