# 出行规划小助手 · 产品需求文档（PRD）

> Day 4 产出 ｜ 项目：`meng-vibe-coding` ｜ 日期：2026-09-19 ｜ 版本：v1.1（Day 4 自检后修订）
> 前置文档：`research.md`（Day 3）
>
> **这份文件是干什么的**：research.md 回答"**值不值得做**"，PRD 回答"**到底做成什么样**"。
> 从 Day 5 开始，AI 写的每一行代码、你每天的验收，**都以本文件为准**。

**三句话摘要**

1. 它是一个**单页网页**：输入城市、天数、预算 → 输出一套行程方案。
2. 核心价值是**算钱 + 按预算倒推**，不是找最便宜的方案。
3. 不做订票、不做实时票价、不上后端（数据全在浏览器本地）。

---

## 一、这个产品给谁用、解决什么

### 目标用户

- 大学生、刚工作的年轻人
- 有**整块假期**（周末 / 小长假 / 寒暑假），预算有限
- 愿意用时间换一点钱，但**不接受"为了省钱把自己搞得很累"**

### 核心场景（三个）

| # | 场景 | 用户的原话 |
|---|------|-----------|
| 1 | 小长假想去好几个城市，**不知道每个城市待几天** | "我想去成都和重庆，7 天怎么分？" |
| 2 | 有预算，**不知道够不够** | "我就 2000 块，这趟够吗？" |
| 3 | 怕自己排的方案**又贵又累** | "有没有既花得起、又不用天天赶车的方案？" |

### 一句话价值

> 给我**几个城市、几天、一个预算**，我排出**一套花得起、也不难受的行程**，并把"钱花在哪、超没超"说明白。

### 用了之后，差别在哪

| 现在（没有这个工具） | 用了之后 |
|---------------------|---------|
| 翻攻略、凭感觉定天数 | 天数自动分好，**每天都有具体安排** |
| 不知道总共要花多少，不敢出发 | 交通 / 住宿 / 吃 / 门票**四项清清楚楚** |
| 只知道"大概两千"，回来才发现超了 | **一开始就知道**超没超，超了还告诉你砍哪一项 |

### 明确不解决的

订票、实时票价、实时房价、账号登录、多人协作、地图导航 —— 完整清单和原因见**第五节**。

---

## 二、用户看到什么（页面结构）

**单页**：所有内容都在同一页，从上到下 4 块。点按钮后**页面不跳转**，结果出现在下面。

```
┌──────────────────────────────────────────┐
│ ① 标题区：产品名 + 一句话说明             │
├──────────────────────────────────────────┤
│ ② 输入区：4 个字段 + 「生成方案」按钮      │
├──────────────────────────────────────────┤
│ ③ 结果区（点按钮前完全不显示）            │
│    A. 每日行程                            │
│    B. 城际交通方案对比                    │
│    C. 花费清单（四项小计 + 合计）          │
│    D. 预算结论 + 超支建议                  │
│    E. 美食美景清单（可筛选「吃/玩」+自填） │
├──────────────────────────────────────────┤
│ ④ 底部：数据说明「估算值，仅供参考」       │
└──────────────────────────────────────────┘
```

### ② 输入区：4 个字段的确切定义

| 字段 | 类型 | 规则 | 默认值 |
|------|------|------|--------|
| **出发城市** | 文本输入框 | 必填，1–10 个字 | 空 |
| **想去的城市** | 从列表选择 + 可手动输入 | 必填，**1–4 个**。内置可选：成都、重庆；其他城市手动打字添加 | 空 |
| **玩几天** | 数字输入框 | 必填，整数，**2–20** 天 | 空 |
| **总预算** | 数字输入框 | **选填**，单位「元/人」。留空 → 只算总花费，不做预算对比 | 空 |
| **生成方案** | 按钮 | 点击 → 先校验 → 通过则生成结果 | — |

### ③ 结果区：五块内容的确切样子

**A. 每日行程**（每天一行）

| 天数 | 城市 | 当天大致安排 | 当天花费估算 |
|------|------|-------------|-------------|
| 第 1 天 | 成都 | 抵达 + 宽窄巷子 / 人民公园周边 | 约 220 元 |
| 第 2 天 | 成都 | 市区游玩（大熊猫基地等） | 约 200 元 |
| … | … | … | … |

**B. 城际交通方案对比**（每段把**可行的方式都列出来**）

**同一路段列出几种方式**（高铁 / 普速列车 / 长途汽车 / 拼车），用 **✓** 标出**本方案选的那个**：

| 本方案选用 | 方式 | 耗时 | 单人票价 | 说明 |
|-----------|------|------|---------|------|
|  | 高铁（最快） | 约 6.1 小时 | 约 535 元 | 最快，也最贵 |
| **✓** | 普速列车（最省） | 约 17.5 小时 | 约 180 元 | 慢很多，但便宜；过夜车次还能省一晚住宿 |

> **Day 7 之后的调整（本文档已同步）**：原来每段只给"一种最便宜的方式"，Day 7 改成 **把可行方式都列出来让用户自己挑**，并给出**两套整体方案**（最省 / 性价比）供一键切换。所以「路段」不再是第一列，第一列改成「本方案选用」。

每段下面**必须有一句结论**，例如："这段坐高铁比普速快 10 小时、多花 300 元。"

**C. 花费清单**（分类小计 + 合计）

| 项目 | 计算口径 | 金额 |
|------|---------|------|
| 城际交通 | 各段票价合计 | ¥xxx |
| 市内交通 | 20 元/天 × 天数 | ¥xxx |
| 住宿 | 90 元/晚 ×（天数 − 1） | ¥xxx |
| 吃 | 80 元/天 × 天数 | ¥xxx |
| 门票 | 60 元/天 × 天数 | ¥xxx |
| **合计** | | **¥xxxx** |

> 上表里的 20 / 90 / 80 / 60 是**学生档参考价**，实现时集中放在一份"价目表"里，方便以后统一调整；所有数字**必须标注为估算值**。
>
> **口径说明（必须写死，否则实现时会算错）**：上表的「天数」一律指**总天数（含在途日）**，不是游玩天数——在途那天你照样要吃、要住。住宿按「**总天数 − 1**」晚计算。

**D. 预算结论**（一行结论 + 建议）

- 在预算内 → 显示「**预算内，还剩 ¥XXX**」（用醒目的正向样式）
- 超出 → 显示「**超出 ¥XXX**」+ **至少 1 条**具体建议（规则见 F6）

**E. 美食美景清单**（每个城市一小段）

| 类型 | 名称 | 参考花费 |
|------|------|---------|
| 吃 | 成都 · 龙抄手 | 约 20 元/人 |
| 玩 | 成都 · 人民公园 | 免费 |

- 内置城市（成都、重庆）→ 显示清单
- **没有内置数据的城市** → 显示「暂无数据，可点这里自己添加」+ 添加表单（名称 / 类型：吃或玩 / 花费）

---

## 三、点了会怎样（功能与规则）

### 功能清单

| 编号 | 功能 | 谁做 |
|------|------|------|
| F1 | 输入校验 | 点"生成方案"时自动执行 |
| F2 | 生成方案（总调度） | 点"生成方案"按钮 |
| F3 | **分天数** | 自动 |
| F4 | **排城际交通 + 市内交通** | 自动 |
| F5 | **算总账** | 自动 |
| F6 | 预算对比 + 超支建议 | 自动 |
| F7 | 美食美景清单 | 自动（读内置数据） |
| F8 | 用户自己添加地点/花费 | 用户点"自己添加" |
| F9 | 美食美景清单筛选（全部 / 只吃 / 只玩） | 用户点筛选按钮 |

### F1 输入校验

点"生成方案"后，按顺序检查，**任何一项不过就停下并在该字段下方显示红色提示文字**（不弹窗、不跳转、不清空已填内容）：

| 编号 | 情况 | 提示文字 |
|------|------|---------|
| E1 | 出发城市为空 | 请填写出发城市 |
| E2 | 没选任何目的城市 | 请至少选择 1 个想去的城市 |
| E3 | 天数不是 2–20 的整数 | 天数请填 2 到 20 之间的整数 |
| E4 | 城市数量 > 可用于游玩的天数（已经扣掉路上的整天） | 去 N 个城市至少需要 N 天（不含路上），请增加天数或减少城市 |
| E5 | 预算填了但不是正数 | 预算请填一个大于 0 的数字（不想设预算可以留空） |

### F2 生成方案（总调度）

点「生成方案」后按**固定顺序**执行：

**F1 校验 → F3 分天数 → F4 排交通 → F5 算总账 → F6 预算对比 → F7 出清单**

- 顺序不能换：没分完天数就不知道住几晚，不知道住几晚就算不出总账。
- 任何一步没过关，就**不生成半成品**，停在输入区显示提示。

### F3 分天数（核心逻辑，必须实现）

1. **先算每段城际交通占用几天**：一段交通耗时 **≥ 6 小时 → 占用 1 天**；**< 6 小时 → 不占用整天**（当天上午玩、下午赶路，仍能安排活动，行程里会注明"半天在路上"）。

   > **为什么用"整天"而不是"半天"**：每日行程是**一天一行**的。如果路上只占半天，那这一天到底算哪个城市就说不清，AC6 也没法验。宁可粗一点，也要保证"每天都有明确归属"。

2. **可用于游玩的天数** = 总天数 − 各段占用天数之和。
3. **先做一次校验**：可用于游玩的天数 **< 城市数** → 停下，提示 E4。
4. **平均分配**：可用于游玩的天数 ÷ 城市数，向下取整得基数，**余数按城市顺序从前到后各加 1 天**。
5. 结果写进「每日行程」表：**占用 1 天的交通段单独成行**，写「**在途：出发地 → 到达地**」；其余每天落到具体城市，**不能有空白天**。

> **算例（就是本 PRD 的验收示例）**：武汉 → 成都 → 重庆 → 西安，7 天。
> 三段交通按**最快方式（高铁）**算约 6.1h / 1.6h / 3.6h → 只有第一段 ≥ 6 小时，**占用 1 天** → 可用于游玩 **7 − 1 = 6 天** → 3 个城市各 2 天。
> 排出来：
>
> | 第 1 天 | 第 2–3 天 | 第 4–5 天 | 第 6–7 天 |
> |---|---|---|---|
> | 在途：武汉 → 成都 | 成都 | 重庆 | 西安 |
>
> **⚠️ 补充（Day 7 加了"交通方式组合"之后必须写清）**：占用几天**取决于选哪套方案** —— 慢车会跨过"6 小时"这条线。同一组输入现在会出两套：
>
> | 方案 | 各段怎么选 | 在途 | 可游玩 | 总合计 |
> |------|-----------|------|--------|--------|
> | **最省方案** | 三段都挑最便宜的（普速） | **2 天** | 5 天 | **¥2005** |
> | **性价比方案** | 在途那段换回快的 | **1 天** | 6 天 | **¥2220** |
>
> 上面那张表对应的是**性价比方案**的排法。**最省方案**里重庆 → 西安选普速要 10.3 小时（≥ 6h），也占 1 天，所以排法是：在途 → 成都 ×2 → 重庆 ×2 → **在途** → 西安 ×1。
>
> **一共 7 行，一天不多、一天不少**，和 AC6 对得上。

### F4 排城际交通

- 每段城市之间从"参考价目表"里取交通方式（高铁 / 普速列车 / 大巴 / 拼车等）的**耗时和票价**。
- 同一段如果有多种方式，**列出来做对比**，并给一句"省多少钱 vs 多花几小时"的结论。
- 市内交通不单独排版，直接按 **20 元/天** 计入花费清单。

### F5 算总账

按第二节「C. 花费清单」的口径逐项相加，输出四项小计 + 合计。

### F6 预算对比 + 超支建议

- 合计 ≤ 预算 → 显示"预算内，还剩 ¥XXX"。
- 合计 > 预算 → 显示超出金额，并**按顺序尝试**给出建议，命中即显示，最多显示 3 条：

| 顺序 | 建议内容 | 触发条件 |
|------|---------|---------|
| 1 | "把 A → B 这段改成夜车/卧铺，省下 1 晚住宿，约 ¥90" | 该段有夜间车次选项 |
| 2 | "住宿从快捷酒店换成青旅，每晚省约 ¥40，共省 ¥XXX" | 住宿费 > 0 |
| 3 | "去掉最远的 X 城，可省城际交通约 ¥XXX + 住宿约 ¥XXX" | 城市数 ≥ 2 |
| 4 | "最低可行预算约 ¥XXX。这套行程按当前预算较难压下来，建议换目的地或增加预算" | 以上三条加起来仍不够 |

### F7 美食美景清单

- 内置成都、重庆两城的清单（**每城 6 条：3 吃 + 3 玩**），每条都带参考花费。
- 清单里的花费**一律不计入总花费合计**。原因：合计里的"吃"和"门票"已经按"每天人均"算过一遍了，清单再算一遍就是**重复计算**。
- 清单下方固定显示一行：「**参考**：以上推荐的花费仅作参考，未计入上面的合计」。

### F8 用户自己添加地点/花费

- 每个没有内置数据的城市，提供"自己添加"入口。
- 添加一条需要：**名称 + 类型（吃 / 玩）+ 参考花费**（花费可留空）。
- 添加后立刻显示在该城市的清单里，并在清单下方单独列一行「**自填地点（参考）**」显示小计。
- **口径与内置清单完全一致：自填地点同样不计入总花费合计**（都是参考值，不能一边不计入、一边计入）。

### F9 美食美景清单筛选

- 清单顶部有一排**三档**筛选按钮：`全部` / `只吃` / `只玩`，默认停在「全部」——也就是跟没有筛选时**完全一样**。
- 点某一档 → 清单立刻只留这一类条目，并在旁边显示**命中条数**（例：「筛出 7 条」）。
- **某座城市在这一档下没有条目时，绝不能留空白**，要写清楚三件事：这个筛选下没有条目 + 该城一共有几条且都属于另一类 + 点「全部」就能看回来。
  （用户看到空白会以为页面坏了。这是本功能最容易漏的一条。）
- 筛选**只管「显示」**：切筛选不改动任何数字，**不影响总花费合计**，口径与 F7 / F8 完全一致。
- 键盘和读屏要能用：按钮是真 `<button>`（Tab 能到、回车能触发）；当前档位带 `aria-pressed`；命中条数放在 `aria-live` 里自动播报。
- **做这类交互，一律照 `.workbuddy/skills/filter-interaction/SKILL.md` 的 5 条规矩走**，改完跑它自带的检查脚本（见 `TECH_DESIGN.md` 第八节 约束 5）。

---

## 四、什么算做完（验收标准）

**这一节是 Day 7 之后每天的验收依据。每条都必须能"打勾或打叉"，不含形容词。**

| 编号 | 验收标准 | 怎么验 |
|------|---------|--------|
| AC1 | 双击 `index.html` 就能在浏览器里打开，不需要安装任何东西 | 实际双击一次 |
| AC2 | 打开后 3 秒内能看到输入区，且**四个字段都在**（出发城市 / 想去的城市 / 玩几天 / 总预算） | 打开页面数一眼 |
| AC3 | 出发城市、天数、预算可以打字输入；想去的城市能**从列表里选**，也能**手动输入新城市** | 手动试一遍 |
| AC4 | 出发城市留空时点"生成方案"，页面出现「请填写出发城市」这句提示，**页面不跳转、不报错**；再输入「武汉 → 成都、西安，2 天」（路上就要占 1 天，时间不够），出现「去 2 个城市至少需要 2 天…」提示 | 故意留空点一次；再故意填一组不够用的极端值 |
| AC5 | 输入「武汉 → 成都、重庆、西安，7 天，2000 元」点生成，**3 秒内**出现结果，且五块内容（每日行程 / 交通对比 / 花费清单 / 预算结论 / 美食清单）**全部出现** | 用这组数据跑一次 |
| AC6 | 每日行程的**每一行都有明确归属**——要么是具体城市，要么写着「在途：A → B」；没有空白天；且**行数正好等于输入的 7 天** | 数一遍行数 |
| AC7 | 城际交通每一段都显示：方式、耗时、单人票价，**并且有一句"时间 vs 花费"的结论** | 逐段看 |
| AC8 | 花费清单显示**城际交通 / 市内交通 / 住宿 / 吃 / 门票** 五项小计 + 合计，**合计等于各项相加**；且清单里**不含**任何"自填地点"等参考项 | 手动加一遍核对 |
| AC9 | 合计 ≤ 预算时显示"预算内，还剩 ¥XXX"；合计 > 预算时显示超出金额，**且至少给出 1 条具体建议** | 分别用 2000 元和 800 元测两次 |
| AC10 | 成都、重庆的清单里**各至少有 6 条**，每条都有名称、类型、参考花费 | 数一遍 |
| AC11 | 给一个没有内置数据的城市（如西安），页面显示「暂无数据，可自己添加」；**添加一条后立刻出现在清单里**，并在清单下方出现「自填地点（参考）」一行；**该行不计入总花费合计** | 实际添加一条试试 |
| AC12 | 页面底部能看到「**估算值，仅供参考**」的字样 | 滚到底看 |
| AC13 | 把浏览器窗口缩到手机宽度，**内容仍能读完，不需要左右拖动** | 拖窗口试一次 |
| AC14 | **断网状态下刷新页面**，**核心流程**照常可用 —— 填表、生成方案、看行程 / 交通 / 花费 / 预算结论，全部正常（说明核心功能没有依赖任何联网数据） | 断网刷新一次，完整走一遍流程 |
| AC15 | **首页的真实地图视图**（第二版才做）是全站**唯一**的联网功能：在线时可用；**离线时按钮置灰 + 写明"需要联网"**，不报错、不影响其他任何功能 | 断网后打开首页，看那个按钮是不是灰的、有没有提示 |
| AC16 | 美食美景清单顶部的筛选按钮：点「**只吃**」后清单只剩「吃」的条目、并显示命中条数；**某城在该档下没有条目时显示「这个筛选下没有条目」而不是一片空白**；点「**全部**」恢复成筛选前的样子，一条不多、一条不少 | 三档各点一次，并挑一个"该档下没有条目"的城市专门验一次 |

### 两条容易被忽略、但会锁死 Day 5 选型的验收标准

**AC1（双击就能打开）和 AC14（断网可用）不只是验收标准，它们同时是技术选型的硬约束：**

- **不能选需要"先构建才能跑"的方案**（比如 Vite / React 工程模板）——那样双击 `index.html` 是打不开的
- **不能用 CDN 引入外部库**（从网上加载样式框架之类的）——断网就白屏

Day 5 做技术选型时，必须同时满足这两条。

> **AC15 是唯一的例外**：它必须联网，所以被明确排进第二版，而且必须做成"**离线时置灰**" —— **不能因为一个附加功能联网失败，把整个页面的离线能力拖下水**。

---

## 五、本期砍掉的功能，以及为什么砍

**这一节回答今天要掌握的那个问题：「你在 PRD 里砍掉了什么功能？为什么砍它？」**

| 砍掉的功能 | 为什么砍 |
|-----------|---------|
| 订火车票 / 机票 / 酒店 | 涉及真实交易，需要第三方票务接口、企业资质和支付渠道 |
| 实时票价、余票、房价 | 数据源要么收费、要么禁止爬取，拿不到合规数据 |
| 实时房价浮动（节假日涨价） | 同上。本期只能提示"旺季住宿会上浮"，给不出真实价格 |
| 用户账号 + 云端同步 | 需要后端和数据库，第 23 天才接触；本期数据全在浏览器本地 |
| **保存历史方案** | 会多花一天去处理本地存储，而"能不能跑通"比"能不能存"更紧急 |
| 多人协作编辑行程 | 需要实时同步和冲突处理，复杂度远超本期 |
| 地图与导航 | 高德/百度级别的基建，不重复造 |
| 全国城市的美食美景数据库 | 内容量极大、没有干净数据源。改为**内置成都、重庆 + 用户自填** |
| 用 AI 实时生成推荐 | 需要 API Key、费用和后端，本期做不了 |
| 手动微调每个城市的天数 | 交互复杂度不小，先把"自动分配"跑通 |

**一句话**：**砍掉的都是"没有它也能跑通核心"的东西**。核心是"输入 → 排方案 → 算钱 → 对预算"，这四步不能被砍。

---

## 六、第二版再做（今天不碰）

1. **美食美景清单的完整版**（覆盖更多城市、内容更丰富、带图片）
2. **手动微调每个城市的天数**（拖一下就能改，立刻重算花费）
3. **保存历史方案**（存在浏览器本地，关掉再打开还能看到）
4. **多套方案对比**（同一组输入，给"省钱版 / 舒适版"两个方案并排看）
5. **接真实数据**（票价接口、地图距离自动计算）
6. **首页示意路线地图**（首页正中间放一张地图，把要去的城市用虚线串起来）—— 用 **SVG 矢量手绘**，**离线可看**，不走任何网络请求，所以**不违反 AC14**；配套的城市照片用**本地文件**（`images/` 目录，见 `TECH_DESIGN.md` 第五节），同样不联网。风格目标：**卡通、有美学感**
7. **真实地图视图**（可选腾讯 / 高德 / 百度 / 天地图）—— **这一项必然联网**，是全站唯一的联网功能，对应 **AC15**：离线时按钮置灰并提示"需要联网"

---

## 附：验收用的示例输入

> 出发城市：**武汉**
> 想去的城市：**成都、重庆、西安**（前两个有内置数据，西安演示"自填"）
> 玩几天：**7 天**
> 总预算：**2000 元**

这组数据同时能验到 AC5–AC11，**截图和演示都用它**。

**用它跑出来的正确结果（用来对答案）**

默认显示的是**最省方案**（在途 2 天 / 游玩 5 天 / 总合计 **¥2005**）：

| 第 1 天 | 第 2–3 天 | 第 4–5 天 | 第 6 天 | 第 7 天 |
|---|---|---|---|---|
| 在途：武汉 → 成都 | 成都（含熊猫基地） | 重庆（含洪崖洞） | 在途：重庆 → 西安 | 西安（无内置数据，演示自填） |

切到**性价比方案**（在途 1 天 / 游玩 6 天 / 总合计 **¥2220**）：

| 第 1 天 | 第 2–3 天 | 第 4–5 天 | 第 6–7 天 |
|---|---|---|---|
| 在途：武汉 → 成都 | 成都（含熊猫基地） | 重庆（含洪崖洞） | 西安（无内置数据，演示自填） |

**花费清单（最省方案的准确值，Day 10 实测）**：

| 项目 | 计算口径 | 金额 |
|------|---------|------|
| 城际交通 | 本方案各段所选方式的票价合计（180 + 55 + 110） | ¥345 |
| 市内交通 | 20 元/天 × 7 天 | ¥140 |
| 住宿 | 90 元/晚 × 6 晚 | ¥540 |
| 吃 | 80 元/天 × 7 天 | ¥560 |
| 门票 | 60 元/天 × 7 天 | ¥420 |
| **合计** | | **¥2005** |

- 两套方案**都共 7 行**（AC6）
- 城际交通 3 段，每段都要有"时间 vs 花费"的结论（AC7）
- 西安那一段要出现「暂无数据，可自己添加」（AC11）
- 预算填 **2000 元** → 最省方案**超支 5 元** → 按"零钱级提醒"（超支 ≤ 50 元）给一句"少买点纪念品就回来了"，**不劝你改行程**

---

## 附二：AI 自检记录（Day 4 第③步）

Day 4 的完成标准要求"AI 自检挑不出**必须修改**级问题"。本文件经一轮自检，**4 项必须修改、3 项建议修改已当场改掉**：

| 级别 | 发现的问题 | 处理 |
|------|-----------|------|
| **必须修改** | F3 的算例算错了：有效 5 天 + 路上 1.5 天 = **6.5 天 ≠ 7 天**，和 AC6「行数正好等于 7 天」直接打架 | 重写 F3：交通占用改为**整天**粒度（≥6h 占 1 天，<6h 不占整天），算例重算为「1 天在途 + 6 天游玩」 |
| **必须修改** | "半天在路上"落不进"一天一行"的行程表，说不清那天算哪个城市 | 同上；占用 1 天的交通单独成行写「在途：A → B」，AC6 同步改写 |
| **必须修改** | F7 说清单花费**不计入**合计，F8 说自填花费**计入**合计——同一件事两套口径 | 统一为"参考项一律不计入合计"，F7 / F8 / AC8 / AC11 同步改 |
| **必须修改** | 花费清单里的「天数」没写清含不含在途日，住几晚会有歧义 | 明确「天数 = 总天数（含在途日）」，住宿按总天数 − 1 晚 |
| 建议修改 | F2（总调度）只有一行，没写执行顺序 | 补上 F1→F3→F4→F5→F6→F7 的固定顺序 |
| 建议修改 | AC1 / AC14 会锁死 Day 5 技术选型，但 PRD 里没点明 | 新增一节明确这两条是选型硬约束 |
| 建议修改 | F7 写"约 6 条"、AC10 写"至少 6 条"，验收时会有争议 | 统一为"每城 6 条（3 吃 + 3 玩）" |
| 可选（不改） | AC13 只要求"不需要左右拖动"，没说结果表怎么适配窄屏 | 留给 Day 7 实现时处理 |

**结论**：本文档**已无"必须修改"级问题**，可以作为 Day 5 之后每天的验收依据。

---

*本文件为 Day 4 产出，随项目一起提交进仓库。*
