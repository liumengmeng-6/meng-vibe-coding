# 项目规则（AGENTS.md）
我每天会把「今日任务清单」整块发给你，那是我今天要做的全部内容。请严格按下面的规则配合我，不要做清单以外的事。

## 一、任务边界（最重要）
1. 收到我的任务清单后，**这份清单就是今天唯一的任务范围**，只做清单里写的事。
2. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好，先跟我说一句，等我点头。
3. 不提前做以后的天数。只有我把某天的清单发给你、并明确说「现在开始做 Day X」，才允许动那天。
4. 拿不准是否在范围内，先问我，不要猜。

## 二、交互式推进
1. 收到清单后，先只给我今天的执行计划：按什么顺序做、每步怎么做、哪些要我自己动手。**写完停下，等我确认。**
2. 确认后**一次只做一个步骤**，绝不连着往下做。
3. 每步做完立刻停下，告诉我三件事：做了什么、改了哪些文件、怎么验证（附我能亲眼确认的结果）。
4. 然后**停下等我回复「进入下一板块」**。我没说这四个字，你就不要继续。
5. 「进入下一板块」只对当前这一步有效，不代表可以一路做到收尾。

## 三、人机分工
1. 需要我人工操作的（注册账号、点按钮、装软件、截图等）：不要替我做，也不要假装已完成。只告诉我：这步在干什么、一二三步怎么做、做完我该看到什么、容易踩什么坑，然后停下等我。
2. 需要你生成的内容（写文档、建文件、写代码）：**等我说了再做**，不要在我刚说「开始吧」时就把整天的东西生成出来。
3. 需要拍板的地方（选方案、起名字、定风格），把选项和各自代价列给我选，不要自己决定。
4. 验证结果要我能亲眼确认（页面能打开、文件能看到、命令有输出），不接受「已经完成了」这种口头结论。

## 四、讲清楚环节（我是来学的）
1. 每个步骤开始前，先告诉我：它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用「做了什么 → 结果 → 为什么」讲明白，不要只给结果。
3. 我不懂的概念（命令、报错、术语、配置），一两句话解释清楚，不要默认我会。
4. 报错时先告诉我错在哪、可能什么原因、打算怎么修，我同意后再动手。不要输出教程式长文。

## 五、Git 与提交
1. 我授权你代为执行 git 提交和推送，但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务，等我确认后再提交。
2. commit 信息格式：`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天的任务全部做完、我核对完文件清单之后才提交，中途不要边做边提交。

## 六、当日收尾
1. 我说「今天做完了」之后，请你逐条对照清单里的「完成标准」，输出一张表：完成标准 / 状态（已完成·部分完成·未完成）/ 证据（文件、页面、命令输出）。
2. 核对必须诚实，没做完、做了一半、跳过没做的，直接标出来并说明原因，不要为了凑「全部完成」而模糊处理。
3. 有未完成项时，告诉我还差什么、接不接着做，**由我决定**，不要自己往下做。
4. 全部完成时，按清单的「收尾指令」整理当天成果，提醒我该交哪张截图、该在群里回什么。

## 七、出问题的时候
1. 我要求撤销时，用安全回退（`git revert`，保留历史），禁止 `git reset --hard` 和强制推送。
2. 回退前先和我确认恢复到哪个提交，回退后列出当前状态让我核对。
3. 同一个操作反复失败时停下来报告，说清楚试了什么、报什么错、你判断的原因，不要循环重试。

---

## 八、我追加的规则（Day 6）

> 下面四条是我在 Day 6 追加的，每一条都对应前面几天真实踩过的坑。

### 8.1 环境验证必须落到「我的终端」（防：AI 说自己那边好了，我这边其实没有）

1. 凡是验证「某个东西装好了没、能不能用」（Node、Git、软件、命令），**不许只看 AI 自己环境里的结果**，必须在**我自己开的终端/窗口**里验证。
2. 如果 AI 无法直接访问我的终端，就**明确说"这步只能你自己验"**，并把命令和预期输出给我，等我回报结果，不许先下结论。
3. 结论要写清是在**哪个环境**验证的。例：「WorkBuddy 内置环境里有 Node，不等于我的系统装了 Node」。
4. 一旦发现之前的结论有误，**立刻主动纠正并说明影响范围**，不要含糊过去。

> **这条防的是 Day 1 那次**：AI 在它自己的环境里跑通了 `node -v` 就说"Node.js 已装"，结果我在自己的 cmd 里敲 `node` 报「不是内部或外部命令」——两套环境根本不是一回事。

### 8.2 提交前列清单，且每个文件标注所属天数（防：混淆哪天做了什么）

1. 每次 `git add` 之前，**必须先列出本次会提交的全部文件**，并逐个标注它属于哪天的任务。
2. 文件如果**不属于今天**（比如今天顺手改了前几天的文档），要**单独说明原因**，等我点头才一起提交。
3. **禁止一把梭 `git add .` / `git add -A`**，必须逐个指定文件名。
4. 提交后要能回答："这条提交里，哪个文件是哪天的产物"。

> **这条防的是**：改了 A 文件、顺手把 B 也提交了，过几天翻历史完全看不出哪天做了什么。

### 8.3 改了一处结论，必须同步所有引用它的文档（防：两份文档自相矛盾）

1. 任何一处**结论性内容**（城市名单、预算口径、功能范围、验收标准、技术选型等）发生变更，**必须主动检查所有引用它的文件**，列出需要同步修改的位置。
2. 同步修改前先告诉我「哪几份文件、哪几处会被改」，等我确认。
3. 提交时，**跨文件的同步修改放在同一条提交里**，并在提交说明中写清"改了什么口径、同步了哪几处"。
4. 项目内互相引用的文档至少包括：`AGENTS.md`、`research.md`、`PRD.md`、`TECH_DESIGN.md`。

> **这条防的是 Day 4 那次**：`PRD.md` 已经定了「内置成都、重庆」，可 `research.md` 里还写着「待定」，两份文档互相打架，以后翻起来一定晕。

### 8.4 清单外的新想法只记录、不落地（防：临时需求把当天任务带偏）

1. 我在清单之外提出的新想法、新需求、新风格要求，**默认只记录，不当天动文件**。
2. 记录时要写清：**想法是什么、我的原话、待办挂在哪天、有哪些约束或代价**。
3. 只有在**我明确说「可以动文件 / 现在就做」**之后，才允许动手；含糊的表述（如"记一下"、"你觉得呢"）不算授权。
4. 如果我认为这个想法会影响已完成文档的口径，**只提醒，不擅自改**，等我决定处理时间。
5. 每天收尾时，把当天记下的清单外想法**列出来给我看**，让我决定排队还是丢弃。

> **这条防的是 Day 5 那次**：我做任务的中途提出"首页要有卡通地图"，如果当时直接动文件，当天该做的技术设计就会被带偏——正确做法是先记录、先讨论可行性和代价，等我定了排期再说。

### 8.5 文件存放与命名规范（防：文件一多就成一锅粥）

1. **项目根目录只放三类东西**：文档（`.md`）、页面代码（`.html` / `.css` / `.js`）、静态资源目录（`images/`）。试稿、备份、临时文件**一律不留在根目录**。
2. **代码文件命名一律用英文小写 + 短横线**，不用中文、不用空格、不用大写。（例：`trip-planner.html`，不写 `出行规划.html`、不写 `Trip Planner.html`）
3. 文档类文件（`.md`）**保留可读的中文名**（如 `PRD.md`、`research.md`），方便我在文件管理器里一眼找到。
4. 城市照片放在 `images/城市拼音/` 下，文件名用**含义**而不是编号（`chengdu-hotpot.jpg`，不写 `1.jpg`）。
5. **AI 不许自己新建文件或目录**——要建任何新文件，必须先说明：建什么、放在哪、干什么用，等我点头才建。

> **这条防的是**：Day 1–6 我只产出了几个 `.md` 文档，名字随意也不影响；但 Day 7 开始写代码后，文件会从 6 个暴涨——照片、草稿、备份一旦全堆在根目录，找 `app.js` 都要翻半天。另外中文文件名在命令行里极易乱码（Day 1 把文件夹名的空格改成短横线就是这个道理）。

### 8.6 语言与讲解偏好（防：默认我会、话说得太绕）

1. **全程中文回答**，不夹杂英文长句。
2. 专业名词**第一次出现时必须跟一句"人话解释"**。例：「`defer` —— 就是"等页面读完再执行这段代码"」。
3. **不默认我懂任何东西**。看到我可能没听过的词（命令、报错、缩写），要**主动解释，不要等我来问**。
4. 回答**先给结论、再给理由**，不要铺垫一大段才说到重点。
5. **不确定的事必须注明**「待核实 / 估算值 / 我不确定」，不要含糊过去。

> **这条防的是**：我原来的规则里只写了"我不懂的概念一两句话解释清楚"，但那是"**我问了才答**"。实际这几天很多概念是 AI 主动帮我翻译的（竞品、验收标准、示意地图），我才没卡住——写成"主动解释"比等我开口更省力。第 5 条尤其重要：我的产品给的是"估算花费"，如果把估算说得像实价，用户会当真。

### 8.7 数据必须有出处，估算必须标注（防：一个数字错了，整个方案就不可信）

1. 项目里任何**参考价、耗时、门票价、住宿价**，都必须注明**来源和日期范围**，或明确标成「估算值」。
2. **不许凭空编造数据当事实用**。拿不到的数据就写「待核实」，并说清卡在哪、什么时候能补。
3. 数据要**集中放在一处**（价目表 + 城市清单），不散落在代码各个角落——改一次只改一个地方。
4. 页面上凡是估算的数字，**必须显示「估算值，仅供参考」**；节假日等会浮动的地方要单独提示。

> **这条防的是**：这是本项目独有的风险点——核心输出就是"这趟花 1980 元"这样的数字，**错一个，整个方案就不可信**。而数据来源是"AI 整理 + 人工核对"，天生带误差（如国庆房价翻倍这类实时数据根本拿不到）。好处是双重的：对内，Day 7 之后每次加数据都有规矩；对外，页面上那句"估算值"就是免责声明，老师问起来答得住。
